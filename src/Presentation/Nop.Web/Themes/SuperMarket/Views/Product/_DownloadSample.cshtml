@model ProductDetailsModel
@if (Model.HasSampleDownload)
{
    <div class="download-sample">
        <a class="button-2 download-sample-button" href="@Url.RouteUrl("GetSampleDownload", new { productid = Model.Id })"><i class="fa-solid fa-download"></i></a>
    </div>
}